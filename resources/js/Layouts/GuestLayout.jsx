export default function GuestLayout({ children }) {
    return (
        <div className="auth-pattern min-h-screen flex items-center justify-center p-8">
            {children}
        </div>
    );
}
